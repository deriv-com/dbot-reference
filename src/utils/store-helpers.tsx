import { Localize } from '@deriv-com/translations';

export const helpers = {
    keep_current_contract: (
        <Localize i18n_default_text='Would you like to keep your current contract or close it?' />
    ),
};
